include(cc_library)
include(cc_test)

cc_library(
  NAME 
    layers
  HDRS 
    model_parallel.h
    norm.h
    linear.h
    embedding.h
    pos_embedding.h
    attention.h
  SRCS 
    model_parallel.cpp
    attention.cpp
    pos_embedding.cpp
  DEPS
    :state_dict
    :memory
    :flash-attn.kernels
    glog::glog
    gflags::gflags
    torch
)

cc_test(
  NAME
    layers_test
  SRCS
    layers_test.cpp
    pos_embedding_test.cpp
  DEPS
    :layers
    :state_dict
    GTest::gtest_main
)
