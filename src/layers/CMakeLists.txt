include(cc_library)
include(cc_test)

cc_library(
  NAME 
    layers
  HDRS 
    norm.h
    linear.h
    embedding.h
    pos_embedding.h
    attention.h
  SRCS 
    attention.cpp
    pos_embedding.cpp
  DEPS
    :torch_utils
    :memory
    :flash-attn.kernels
    glog::glog
    gflags::gflags
    torch
)

cc_test(
  NAME
    layers_test
  SRCS
    layers_test.cpp
    pos_embedding_test.cpp
  DEPS
    :layers
    :torch_utils
    GTest::gtest_main
)
