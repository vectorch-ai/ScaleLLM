include(cc_library)
include(cc_test)

cc_library(
  NAME 
    layers
  HDRS 
    model_parallel.h
    norm.h
    linear.h
    linear_impl.h
    qlinear_impl.h
    embedding.h
    pos_embedding.h
    attention.h
  SRCS 
    linear.cpp
    linear_impl.cpp
    qlinear_impl.cpp
    model_parallel.cpp
    pos_embedding.cpp
    attention.cpp
  DEPS
    :state_dict
    :memory
    :flash-attn.kernels
    :gptq.kernels
    glog::glog
    gflags::gflags
    torch
)

cc_test(
  NAME
    layers_test
  SRCS
    layers_test.cpp
    pos_embedding_test.cpp
  DEPS
    :layers
    :state_dict
    GTest::gtest_main
)
