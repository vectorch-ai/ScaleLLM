include(cc_library)

cc_library(
  NAME 
    llama
  HDRS 
    attention.h
    feedforward.h
    transformer_block.h
    transformer.h
  SRCS 
  DEPS
    :input_parameters
    :model_args
    :layers
    :memory
    glog::glog
    torch
)
