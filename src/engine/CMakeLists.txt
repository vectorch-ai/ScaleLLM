include(cc_library)
include(cc_test)

cc_library(
  NAME 
    engine
  HDRS
    utils.h
    worker.h
    engine.h
  SRCS
    utils.cpp
    worker.cpp
    engine.cpp
  DEPS
    torch
    :common
    :request
    :torch_utils
    :causal_lm
    :logits_processor
    :sampler
    :tokenizer
    :model_args
    :model_loader
    glog::glog
    Folly::folly
    absl::synchronization
)

cc_test(
  NAME
    engine_test
  SRCS
    worker_test.cpp
  DEPS
    :engine
    absl::time
    GTest::gtest_main
)
