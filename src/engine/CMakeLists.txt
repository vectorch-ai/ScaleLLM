include(cc_library)
include(cc_test)

cc_library(
  NAME 
    engine
  HDRS
    utils.h
    worker.h
    engine.h
  SRCS
    utils.cpp
    worker.cpp
    engine.cpp
  DEPS
    torch
    :common
    :request
    :state_dict
    :models
    :logits_processor
    :sampler
    :tokenizer
    :model_loader
    glog::glog
    Folly::folly
    absl::synchronization
)

cc_test(
  NAME
    engine_test
  SRCS
    worker_test.cpp
  DEPS
    :engine
    absl::time
    GTest::gtest_main
)
