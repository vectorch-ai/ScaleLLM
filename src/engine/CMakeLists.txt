include(cc_library)
include(cc_test)

cc_library(
  NAME 
    engine
  HDRS
    engine.h
    worker.h
    concurrent_queue.h
    executor.h
  SRCS
    executor.cpp 
  DEPS
    :request
    glog::glog
    Folly::folly
    absl::synchronization
)

cc_test(
  NAME
    engine_test
  SRCS
    executor_test.cpp
  DEPS
    :engine
    absl::time
    GTest::gtest_main
)
