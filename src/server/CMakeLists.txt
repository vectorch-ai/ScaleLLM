include(cc_binary)
include(cc_library)

cc_library(
  NAME 
    hf_model_downloader
  HDRS 
    hf_model_downloader.h
  SRCS 
    hf_model_downloader.cpp
  DEPS
    ${TORCH_LIBRARIES}
    Python::Python
)

cc_binary(
  NAME 
    simple
  SRCS 
    simple.cpp
  DEPS
    :torch_utils
    :llama
    :tokenizer
    :hf_model_downloader
    ${TORCH_LIBRARIES}
    gflags::gflags
    glog::glog
)
