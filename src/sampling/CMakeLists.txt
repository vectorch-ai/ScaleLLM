include(cc_library)
include(cc_test)

cc_library(
  NAME 
    sampler
  HDRS
    parameters.h  
    logits_processor.h
    sampler.h
  SRCS 
    logits_processor.cpp
    sampler.cpp
  DEPS
    :kernels
    glog::glog
    torch
)

cc_test(
  NAME
    sampler_test
  SRCS
    sampler_test.cpp
    logits_processor_test.cpp
  DEPS
    :sampler
    GTest::gtest_main
)