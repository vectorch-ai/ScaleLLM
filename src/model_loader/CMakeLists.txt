include(cc_test)
include(cc_library)

cc_library(
  NAME 
    state_dict
  HDRS 
    state_dict.h
    tensor_utils.h
  SRCS 
    state_dict.cpp
    tensor_utils.cpp
  DEPS
    huggingface
    torch
    glog::glog
    Folly::folly
)

cc_test(
  NAME
    state_dict_test
  SRCS
    state_dict_test.cpp
  DEPS
    :state_dict
    GTest::gtest_main
  DATA 
    data/test.pth 
    data/test.safetensors
)

cc_library(
  NAME 
    model_loader
  HDRS 
    model_loader.h
    args_overrider.h
  SRCS 
    model_loader.cpp
    args_overrider.cpp
  DEPS
    :common
    :models
    :tokenizer
    torch
)
