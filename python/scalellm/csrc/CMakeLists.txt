include(pybind_extension)

pybind_extension(
  NAME
    _C
  COPTS
    -DPY_MODULE_NAME=_C
  HDRS
    llm.h
  SRCS
    llm.cpp
    scalellm.cpp
  DEPS
    :engine
    torch
    absl::strings
    gflags::gflags
    glog::glog
    Python::Python
)
