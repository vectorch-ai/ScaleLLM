include(pybind_extension)

pybind_extension(
  NAME
    _C
  COPTS
    -DPY_MODULE_NAME=_C
  HDRS
    llm.h
    sampling_params.h
    llm_handler.h
  SRCS
    llm.cpp
    llm_handler.cpp
    scalellm.cpp
  DEPS
    :engine_factory
    :scheduler
    torch
    absl::strings
    gflags::gflags
    glog::glog
    Python::Python
)
