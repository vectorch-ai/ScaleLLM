include(pybind_extension)

pybind_extension(
  NAME
    _C
  COPTS
    -DPY_MODULE_NAME=_C
  HDRS
    llm.h
    _sampling_parameter.h
    _llm_engine.h
  SRCS
    llm.cpp
    _llm_engine.cpp
    scalellm.cpp
  DEPS
    :engine
    torch
    absl::strings
    gflags::gflags
    glog::glog
    Python::Python
)
