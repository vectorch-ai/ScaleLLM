include(pybind_extension)

pybind_extension(
  NAME
    _C
  COPTS
    -DPY_MODULE_NAME=_C
  SRCS
    csrc/sampling_params.cpp
    csrc/output.cpp
    csrc/llm_handler.cpp
    csrc/vlm_handler.cpp
    csrc/module.cpp
  DEPS
    :llm_handler
    torch
    absl::strings
    gflags::gflags
    glog::glog
    Python::Module
)
